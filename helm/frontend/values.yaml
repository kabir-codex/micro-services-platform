replicaCount: 2

image:
  repository: ghcr.io/kabir-devs/microservices-platform/frontend
  tag: "3d739348fc9269de9b2961b990d676a7fb07579c"
  pullPolicy: IfNotPresent

service:
  port: 8080

ingress:
  enabled: true
  host: platform.local
  tlsSecretName: platform-tls

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 128Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70

networkPolicy:
  # Restrict ingress to traffic from the ingress-nginx namespace.
  enabled: true

podSecurityContext:
  runAsNonRoot: true
  fsGroup: 1000

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: false
  capabilities:
    drop: ["ALL"]
